# Directory content

The git repository of this directory only contains example CVs and applications.

This directory contains the files that back the chat for the bewerbungsavatar. They are to be bind mounted into the docker-compose.yml.

